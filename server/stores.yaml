file:
    driver: file
    raw: true
    driveropts:
        parser: text
        Path: /stores/files

env:
    driver: file
    driveropts:
        Parser: yaml
        Path: ./stores/envs
        Suffix: .yaml